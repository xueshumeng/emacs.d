;;; init.el ---                       -*- lexical-binding: t; -*-
;;; Commentary:
;;; Code:

(push (package-path "emacs-which-key") load-path)

(require 'which-key)

(which-key-mode)

(provide 'init-whichkey)
;;; init-whichkey.el ends here
